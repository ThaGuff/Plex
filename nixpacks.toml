[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npx prisma generate",
  "npx prisma db push --accept-data-loss",
  "npx tsx prisma/seed.ts",
  "npm run build"
]

[start]
cmd = "npm run start"
